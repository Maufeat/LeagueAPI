#include <leagueclient/https.hpp>
namespace leagueapi::https {
    Response Do(const Info& info, const std::string& path, const std::string& method, const Args& query, const Args& headers) {
    }
    Response Do(const Info& info, const std::string& path, const std::string& method, const Args& query, const Args& headers, const json& body) {
    }
    Response Do(const Info& info, const std::string& path, const std::string& method, const Args& query, const Args& headers, const Args& formdata) {
    }
}